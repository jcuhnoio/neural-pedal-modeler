# Sliding Window Configuration
WINDOW_SIZE_INPUT = 1  # Number of past input samples to include (0 = current only)
WINDOW_SIZE_OUTPUT = 0  # Number of past output samples to include (0 = current only)